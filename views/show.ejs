
<div class='container-2'> 
   <a href="/notes/new/<%= folder.id %>" class="new">Make a new note by clicking me!</a>
   <h3>These are all of your notes:</h3>
   <% if(folder.notes) { %>
      <% folder.notes.forEach( n => { %> <br><br>
        <a id="title" href="/notes/<%= n.id %>">
         <%= n.title %> </a><br>    
      <form method="POST" action="/notes/<%= n.id %>?_method=DELETE">
         <input type="hidden" name="id" value="<%= n.id %>">
         <button type="submit">Remove</button>
      </form>  
      <% }) %>
      <% } %> 
   </div>

